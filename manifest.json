{
	"manifest_version": 2,
	"name": "derpiclick",
	"version": "1.0",

	"description": "adds a context menu item to open the selected image in derpibooru, if possible.",

	"permissions": [
		"menus",
		"tabs"
	],

	"background": {
		"scripts": ["derpiclick.js"]
	},

	"applications": {
		"gecko": {
			"id": "aucepx@gmail.com"
		}
	}
}
